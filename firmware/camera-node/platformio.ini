; Waggle Camera Node — ESP32-CAM photo capture with WiFi upload
; Wake cycle: camera init -> capture -> WiFi connect -> NTP sync -> HTTP POST -> deep sleep

[env:esp32cam]
platform = espressif32
board = esp32cam
framework = arduino
monitor_speed = 115200
lib_deps =
    HTTPClient
build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
